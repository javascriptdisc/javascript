'use strict';

var x = Math.random()*10; //generates random number between 0 and 1

console.log(x); 

var y = Math.floor(3.42340); //rounds it down to the nearest integer
console.log(y);

var z = Math.floor(Math.random()*1000); //combining both functions to create a RNG
console.log(z);

